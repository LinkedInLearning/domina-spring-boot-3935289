package domina.springboot.verduleria.back;

//import org.springframework.data.annotation.Id; // retirar para pasar de H2 a MySQL

// Nuevos imports para pasar de H2 a MySQL
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;


@Entity
public record Verdura(@Id @GeneratedValue(strategy=GenerationType.AUTO) long id, String nombre, Double precio, boolean troceable) {
	// public Verdura() { this(0, "", 0.0, false);} // Intento de constructor por defecto
}
